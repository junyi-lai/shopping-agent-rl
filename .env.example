# ShopSimulator and OpenAI-compatible services
SHOPSIM_BASE_URL=http://127.0.0.1:5700

# 学生模型服务(evaluation 入口自动起 vLLM 时也会用到这两个变量)
LLM_BASE_URL=http://127.0.0.1:8000/v1
LLM_API_KEY=EMPTY
SERVED_MODEL_NAME=shopping-agent

# 教师模型(采集与难度标签走 OpenAI 兼容 API,不下载权重)
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=deepseek-v4-flash

# 国内下载模型可开启镜像
# HF_ENDPOINT=https://hf-mirror.com
