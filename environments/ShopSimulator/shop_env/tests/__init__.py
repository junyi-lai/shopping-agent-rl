"""Unit tests for the embedded ShopSimulator Environment v2.1 snapshot.

Run them from ``shop_env`` with the environment's own interpreter:

    ../.venv-shopsim/bin/python -m unittest discover -s tests -t .
"""
