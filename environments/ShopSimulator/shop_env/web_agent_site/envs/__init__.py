"""ShopSimulator Environment v2.1 runtime package.

``web_agent_text_env`` holds the leased session environment; every slot of the
API service shares one :class:`SimServer` so the frozen corpus is loaded once.
"""
